PySide6>=6.6
pyqtgraph>=0.13.3
matplotlib>=3.8
pandas>=2.0
numpy>=1.26
pypdf>=5.0
scipy>=1.11
# Optional Mac-first local AI backend:
# pip install -r requirements-mlx-mac.txt
# Optional — local AI backend (Metal-accelerated on Apple Silicon):
# pip install llama-cpp-python
# Optional — accelerates fallback filtering if SciPy is unavailable:
# pip install numba
