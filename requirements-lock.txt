# requirements-lock.txt — exact versions of the reference environment that
# Scope Studio is developed and tested against (macOS, Apple Silicon M4 Pro,
# Python 3.14). Use this for a reproducible install:
#
#     pip install -r requirements-lock.txt
#
# Prefer requirements.txt (compatible floors) for normal installs; use this
# lockfile when you need to reproduce the tested setup exactly or to debug a
# version-specific issue.
PySide6==6.11.1
pyqtgraph==0.14.0
matplotlib==3.10.9
pandas==3.0.3
numpy==2.4.6
scipy==1.17.1
pypdf==6.13.2
