__pycache__/
*.pyc
.DS_Store
venv/
.venv/
dist/
build/
*.egg-info/
.pytest_cache/
# local development backups — not part of the shipped project
backups/
# experimental orchestrator project — separate effort, not part of v0.1
orchestrator_lab/
backtests/*.csv
backtests/*.json
backtests/*.png
backtests/*.svg
scope_studio_user_profile.json
user_style_presets.json
*.gguf
# never ship measurement data, logs, or local env files
*.log
.env
*.key
T0*.CSV
T0*.SET
T0*.PNG
*.meta.json
sample_shot.csv
# large design bundles — kept on disk, never in git history (also exist as .zip)
Claidev2/
Claidev2.zip
claude_design_upload/
claude_design_upload.zip
Scope Studio -Claude, UI changes/
Scope Studio -Claude, UI changes.zip
# local style backups and working snapshots
style.qss.bak.*
txt/
__deltest__

# UI preview captures and local http log
scope_web/ui_preview*.png
