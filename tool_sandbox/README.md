# Scope Studio Tool Sandbox

Draft tools created by the local assistant belong in `drafts/`.

Guardrails:

- Draft tools are inactive by default.
- Draft tools run only on in-memory arrays passed by Scope Studio.
- Draft tools must not read, modify, overwrite, or normalize original CSV files.
- Promotion into the approved tool registry requires human review and deterministic tests.

